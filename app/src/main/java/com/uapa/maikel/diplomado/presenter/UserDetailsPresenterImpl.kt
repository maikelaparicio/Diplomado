package com.uapa.maikel.diplomado.presenter

/*
class UserDetailsPresenterImpl(override var view: UserDetailsView?): UserDetailsPresenter<UserDetailsView> {
    override var user: User? = null
        */
/***
         * podemos definir nuestros propios métodos gets y sets que sobrescriben a los que están por defectos o inferidos
          *//*

        set(value) {
            field = value
            if(field != null){
                view?.showUserDetails(field!!)
            } else {
                view?.showNoUserError()
            }
        }

}*/
